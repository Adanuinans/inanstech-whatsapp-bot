services:
  - type: web
    name: inanstech-whatsapp-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main_online.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: MISTRAL_API_KEY
        sync: false
    plan: free
